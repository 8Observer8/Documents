Вопросы по C++

Как работать с статическими переменными в классах C++?

Как использовать remove-erase-idiom из главы 13? Для удаления элементов, которые удовлетворяют условию. Если пробегать в цикле, то это будет квадратичное время, а с этой идиомой - линейное

Чтобы вывести содержимое контейнера на экран, какой вариант лучше?
void printVector(const vector<int>& v) {
    copy(v.begin(), v.end(), ostream_iterator<int>(cout, " "));
    cout << endl;
}

Контейнер QJsonObject.

Нужно ли писать по максимуму на C++ и только в крайней необходимости использовать Qt классы (контейнеры одобтированны для хранения Qt-объектов)